# Increasing Subsequence (easy version) (CF 1157C1)
# Rating  : 1300
# Tags    : greedy
# Lang    : C++14 (GCC 6-32)
# Solved  : 2019-04-27
# Attempt : #2
# URL     : https://codeforces.com/contest/1157/problem/C1

#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int a[n+2];
    for(int i=1;i<=n;i++){
        cin>>a[i];
    }
    int j=0,i=1,k=n;
       int last=INT_MIN;
    vector<char> v;
    while(j<=n && i<=k){
        if(i<=n && a[i]>last && a[k]>last && k>=1){
                        if(a[k]<a[i]){
                v.push_back('R');
               last=a[k];
               k--;
            }else{
                 last=a[i];
                             i++;
                        v.push_back('L');
            }
                                }
       else if(i<=n && a[i]>last ){
                                  last=a[i];
                         i++;
                     v.push_back('L');
                    }else if(k>=1 && last<a[k] ){
                                    last=a[k];
            k--;
                        v.push_back('R');
                    }else{
            break;
        }
        j++;
    }
               cout<<v.size()<<endl;
    for(int i=0;i<v.size();i++){
        cout<<v[i];
    }
    cout<<endl;
        }