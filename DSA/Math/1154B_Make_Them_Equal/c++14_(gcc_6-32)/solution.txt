# Make Them Equal (CF 1154B)
# Rating  : 1200
# Tags    : math
# Lang    : C++14 (GCC 6-32)
# Solved  : 2019-04-16
# Attempt : #1
# URL     : https://codeforces.com/contest/1154/problem/B

#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin>>n;
    int a[n+3];
    int sum=0;
    int dif=INT_MAX;
    set<int> s;
    for(int i=0;i<n;i++){
        cin>>a[i];
        sum+=a[i];
        s.insert(a[i]);
    }
    if(s.size()>2){
             dif=a[0];
   sort(a,a+n);
   for(int i=0;i<n;i++){
        if(i>0 && a[i]!=a[i-1]){
            dif=abs(a[i]-a[i-1]);
            break;
        }
   }
       for(int i=0;i<n;i++){
        for(int j=i+1;j<n;j++)
        {
            if( abs(a[j]-a[i])!=dif && abs(a[j]-a[i])!=0  && abs(a[j]-a[i])!=2*dif){
                cout<<"-1";
                return 0;
            }
                    }
            }
    cout<<dif<<endl;
    return 0;
            }
    else if(s.size()==1){
        cout<<0<<endl;
        return 0;
    }else{
        set<int>:: iterator it;
        sort(a,a+n);
        sum=0;
         for( it = s.begin(); it!=s.end(); ++it){
            sum+= *it;
                     }
        int avg=sum/2;
        if(sum%2==1){
            cout<<abs(a[0]-a[n-1])<<endl;
            return 0;
        }
                      int p=abs(a[0]-avg);
        int q=abs(a[n-1]-avg);
        if(p==q){
            cout<<p<<endl;
            return 0;
        }else{
            cout<<"-1"<<endl;
            return 0;
        }
    }
   }