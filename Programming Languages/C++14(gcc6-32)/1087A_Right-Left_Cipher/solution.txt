# Right-Left Cipher (CF 1087A)
# Rating  : 800
# Tags    : implementation, strings
# Lang    : C++14 (GCC 6-32)
# Solved  : 2018-12-23
# Attempt : #1
# URL     : https://codeforces.com/contest/1087/problem/A

#include<bits/stdc++.h>
using namespace std;
int main(){
    string text;
    //freopen("input.txt","r+",stdin);
    while(cin>>text){
        int len=text.length();
        char f[len+5];
        int x=0;
        if(len==2){
            cout<<text<<endl;
        }
        else if(len%2==0){
            int j=len/2;
            f[x++]=text[j-1];
            int k=j-2;
            for(int i=j;i<len;i++){
                                f[x++]=text[i];
                if(k>=0){
                    f[x++]=text[k];
                }
                              k--;
            }
           f[x]='\0';
            printf("%s\n",f);
            //cout<<x<<endl;
        }
        else{
            int j=len/2;
                       f[x++]=text[j];
            int k=j-1;
            for(int i=j+1;i<len;i++){
                f[x++]=text[i];
               if(k>=0){
                    f[x++]=text[k];
                }
                k--;
            }
           f[x]='\0';
            printf("%s\n",f);
          // cout<<x<<endl;
        }
    }
        }