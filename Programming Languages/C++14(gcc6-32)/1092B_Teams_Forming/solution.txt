# Teams Forming (CF 1092B)
# Rating  : 800
# Tags    : sortings
# Lang    : C++14 (GCC 6-32)
# Solved  : 2018-12-18
# Attempt : #1
# URL     : https://codeforces.com/contest/1092/problem/B

#include<bits/stdc++.h>
using namespace std;
int main(){
     ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    //freopen("intput.txt","r+",stdin);
        int n;
    cin>>n;
    int a[n+2];
    for(int i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a,a+n);
    int sum=0;
    for(int i=0;i<n-1;i+=2){
        sum+=a[i+1]-a[i];
    }
    cout<<sum;
          }