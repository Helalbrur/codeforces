# Exam in BerSU (easy version) (CF 1185C1)
# Rating  : 1200
# Tags    : greedy, sortings
# Lang    : C++14 (GCC 6-32)
# Solved  : 2019-06-19
# Attempt : #1
# URL     : https://codeforces.com/contest/1185/problem/C1

#include<bits/stdc++.h>
using namespace std;
 int main()
{
    int n,s;
    cin>>n>>s;
    int mx=0;
    int a[n+2];
    vector<pair<int,int>> v;
    for(int i=1;i<=n;i++){
        cin>>a[i];
       v.push_back(make_pair(a[i],i));
    }
    sort(v.begin(),v.end());
        for(int i=1;i<=n;i++){
        int sum=0;
        for(int j=1;j<=i;j++){
            sum+=a[j];
        }
        if(sum<=s){
            cout<<0<<" ";
                    }else{
            int cnt=0;
            int flag=0;
            for(int j=v.size()-1;j>=0;j--){
                                int x=v[j].first;
                int pos=v[j].second;
                                if(pos<i){
                    sum-=x;
                    cnt++;
                    if(sum<=s){
                        cout<<cnt<<" ";
                        flag=1;
                        break;
                    }
                }
                            }
            if(flag==0){
                cout<<i<<" ";
            }
        }
                    }
    }